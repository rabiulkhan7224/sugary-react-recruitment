import LoginForm from '@/components/LoginForm';

const page = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default page;
// to do : npm install @hookform/resolvers zod  
